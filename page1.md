# Page 1

Test Chat for Bethany / Advisory Services

<!--
<script>
    // 1. Define your JSON object
    const dummyAuthObject = {
      isImpersonatingUser: false,
      isSwitchedCompany: false,
      impersonateUserShowEnd: false,
      impersonateUserShowStart: false,
      impersonatorToken: {},
      isLoggedIn: true,
      isExpired: false,
      token: {
        access_token: "xxx",
        token_type:  "bearer",
        refresh_token: "yyy",
        expires_in: 3599,
        scope:  "all",
        expires_in_timestamp: 1775154185000,
        userId: 3714255
      },
      loginAttemptStatus: false,
      _persist: {
        version: -1,
        rehydrated: true
      }
  }

    // 2. Convert to string and save to localStorage
    // Key: 'persist:auth_persist', Value: JSON string
    localStorage.setItem('persist:auth_persist', JSON.stringify(dummyAuthObject));

    // 3. Optional: Verify it worked by reading it back
    const storedData = localStorage.getItem('persist:auth_persist');
    const parsedData = JSON.parse(storedData);
    
    console.log("dummy auth object saved to and retrieved from localstorage:", parsedData);
</script>
-->

<script type="module" src="https://us01ccistatic.zoom.us/us01cci/web-sdk/zcc-sdk.js" data-apikey="BwxQFdNDRBy1twW2iKcINg" data-env="us01"></script>
